# Optional. When set, every validated contact-form and newsletter submission is
# POSTed here as JSON so it reaches an inbox, CRM, or automation (Zapier/Make).
# Delivery failures are logged server-side and never block the visitor.
#
# Payload shape:
#   { "kind": "enquiry" | "newsletter", "reference": "GT-260912-A7K3D", ...fields }
ENQUIRY_WEBHOOK_URL=
